{
  "id": "auth-gate",
  "name": "auth-gate",
  "version": "0.1.0",
  "description": "Blocks unregistered users before LLM runs. Zero cost for unauthorized users.",
  "entry": "index.ts",
  "hooks": ["before_prompt_build"],
  "configSchema": { "type": "object", "properties": { "enabled": { "type": "boolean", "default": true } }, "additionalProperties": false }
}
