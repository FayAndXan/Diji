{
  "id": "rule-injector",
  "name": "rule-injector",
  "version": "0.1.0",
  "description": "Core: injects per-user rules, conditional knowledge, and personality every turn.",
  "entry": "index.js",
  "hooks": ["before_prompt_build"],
  "configSchema": { "type": "object", "properties": { "enabled": { "type": "boolean", "default": true } }, "additionalProperties": false }
}
