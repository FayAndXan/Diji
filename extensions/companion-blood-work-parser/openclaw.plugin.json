{
  "id": "companion-blood-work-parser",
  "name": "Companion Blood Work Parser",
  "version": "0.1.0",
  "description": "Detects when images or PDFs might be blood work results and flags to Bryan that blood work processing is needed.",
  "entry": "index.ts",
  "hooks": ["before_prompt_build"],
  "configSchema": {
    "type": "object",
    "properties": {
      "enabled": { "type": "boolean", "default": true }
    },
    "additionalProperties": false
  }
}
